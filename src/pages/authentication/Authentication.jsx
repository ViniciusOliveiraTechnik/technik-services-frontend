import AuthenticationForm from "@/components/authentication/forms/AuthenticationForm";

export default function Authentication() {
  return (
    <section className="bg-gray-100 flex items-center justify-center min-h-screen">
      <AuthenticationForm />
    </section>
  );
}
